# ── VigilAgent .semgrepignore ─────────────────────────────────────────────
# Mirrors the deterministic Policy Scan's exclusions: the repo's own test
# fixtures and demo files carry INTENTIONAL vulnerabilities to prove the
# engines fire — they must never fail the CI security gate.

# Deterministic engine accuracy fixtures (deliberately vulnerable)
**/testdata/
**/testfixtures/
**/testdata/**/*

# Demo / sample files with intentional vulns
sample_*
**/sample_*

# Test-only code
**/*_test.go
**/*_fuzz.go
**/*_bench.go

# Integration test infrastructure (intentional DROP/CREATE on ephemeral DBs)
**/integration/

# Extraction test fixtures (test.go — not _test.go but contains test funcs)
**/extraction/test.go

# Load testing tool (intentional InsecureSkipVerify behind CLI flag)
cmd/loadtest/

# Generated / dependency code
**/vendor/
node_modules/
dist/
coverage/

# CI helper scripts (intentional patterns for scanning demos)
scripts/ci/
